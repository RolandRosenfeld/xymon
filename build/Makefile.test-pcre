include Makefile.$(OS)

test-compile:
	@$(CC) $(CFLAGS) $(PCREINC) $(PCREDEF) -o test-pcre.o -c test-pcre.c

test-link:
	@$(CC) $(CFLAGS) $(PCRELIB) -o test-pcre test-pcre.o -l$(PCRELIBNAME)

clean:
	@rm -f test-pcre.o test-pcre

